/**
 * `helpers sync` command handler.
 * Update existing project from source repo.
 * Two phases: Plan (pure computation) then Apply (side effects).
 */
import type { LockFile } from "../types/lock.js";
import type { RenderedFile } from "../transformers/types.js";
export interface SyncPlanItem {
    action: "add" | "update" | "delete" | "skip";
    path: string;
    reason: string;
    file?: RenderedFile;
}
export interface SyncPlan {
    items: SyncPlanItem[];
    sourceCommit: string;
    sourceRef: string;
}
export declare function computeSyncPlan(lock: LockFile, sourceDir: string, targetNames: string[], options: {
    upgrade?: boolean;
    version?: string;
    ref?: string;
}): Promise<SyncPlan>;
export declare function applySyncPlan(root: string, lock: LockFile, plan: SyncPlan, _sourceDir: string): Promise<LockFile>;
declare const _default: import("citty").CommandDef<{
    upgrade: {
        type: "boolean";
        default: false;
        description: string;
    };
    version: {
        type: "string";
        description: string;
    };
    ref: {
        type: "string";
        description: string;
    };
    "source-config": {
        type: "string";
        description: string;
    };
    "trust-custom": {
        type: "boolean";
        default: false;
        description: string;
    };
    "allow-self-sync": {
        type: "boolean";
        default: false;
        description: string;
    };
}>;
export default _default;
//# sourceMappingURL=sync.d.ts.map