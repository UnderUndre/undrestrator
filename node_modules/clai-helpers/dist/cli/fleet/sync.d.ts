/**
 * `fleet sync` command handler.
 * Syncs clai-helpers across selected GitHub repos.
 */
import type { FleetEntry, Selection } from "../../core/fleet/types.js";
export interface SelectionFlags {
    all?: boolean;
    repo?: string[];
    filter?: string;
}
/**
 * Validate that at most one selection flag is present.
 * Returns an error message string if conflict found, null otherwise.
 */
export declare function validateMutualExclusion(flags: SelectionFlags): string | null;
/**
 * Resolve a non-interactive selection from flags.
 * Must be called AFTER mutual exclusion check passes.
 */
export declare function selectByFlag(entries: FleetEntry[], flags: SelectionFlags): Selection | null;
declare const _default: import("citty").CommandDef<{
    mode: {
        type: "string";
        description: string;
    };
    repo: {
        type: "string";
        description: string;
        repeatable: boolean;
    };
    filter: {
        type: "string";
        description: string;
    };
    all: {
        type: "boolean";
        description: string;
    };
    "patch-output": {
        type: "string";
        description: string;
    };
    yes: {
        type: "boolean";
        alias: string;
        description: string;
    };
    "dry-run": {
        type: "boolean";
        description: string;
    };
    verbose: {
        type: "boolean";
        alias: string;
    };
    "no-color": {
        type: "boolean";
    };
}>;
export default _default;
//# sourceMappingURL=sync.d.ts.map