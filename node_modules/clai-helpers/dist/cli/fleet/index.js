/**
 * `fleet` subcommand registration.
 * Parent command that delegates to subcommands (list, sync, add-org, remove-org).
 */
import { defineCommand } from "citty";
import listCommand from "./list.js";
import syncCommand from "./sync.js";
import addOrgCommand from "./add-org.js";
import removeOrgCommand from "./remove-org.js";
export default defineCommand({
    meta: {
        name: "fleet",
        description: "Manage clai-helpers across multiple GitHub repos",
    },
    subCommands: {
        list: listCommand,
        sync: syncCommand,
        "add-org": addOrgCommand,
        "remove-org": removeOrgCommand,
    },
});
//# sourceMappingURL=index.js.map