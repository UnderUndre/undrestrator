/**
 * Drift detection per FR-018b.
 * Compares local file state against lock file hashes.
 */
import type { SourceEntry, GeneratedEntry } from "../types/lock.js";
export interface DriftResult {
    isDrift: boolean;
    isCustomized: boolean;
    reason?: string;
}
/**
 * Detect drift for a source entry.
 */
export declare function detectSourceDrift(entry: SourceEntry, localContent: string, ext: string): DriftResult;
/**
 * Detect drift for a generated entry.
 */
export declare function detectGeneratedDrift(entry: GeneratedEntry, localContent: string): boolean;
/**
 * Unified drift detection — dispatches by entry kind.
 */
export declare function detectDrift(entry: SourceEntry | GeneratedEntry, localContent: string, ext?: string): DriftResult;
//# sourceMappingURL=drift.d.ts.map