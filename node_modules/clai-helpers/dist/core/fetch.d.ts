/**
 * Source repo fetch wrapper around giget.
 * Auth resolution order per FR-021:
 * 1. Explicit auth param
 * 2. GH_TOKEN env
 * 3. GIGET_AUTH env
 * 4. `gh auth token` subprocess (if gh CLI available)
 */
interface FetchOptions {
    offline?: boolean;
    preferOffline?: boolean;
}
interface FetchResult {
    dir: string;
    commit: string;
}
export declare function fetchSource(url: string, ref?: string, auth?: string, options?: FetchOptions): Promise<FetchResult>;
export declare function resolveAuth(): string | undefined;
export {};
//# sourceMappingURL=fetch.d.ts.map