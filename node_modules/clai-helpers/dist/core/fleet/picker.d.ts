/**
 * Interactive picker for fleet sync.
 * Wraps @inquirer/prompts checkbox + confirm for repo selection.
 */
import type { FleetEntry, Selection } from "./types.js";
/**
 * Present an interactive multi-select checkbox list of fleet entries.
 * Pre-checks entries that have drift (pinned !== latest).
 */
export declare function pickEntries(entries: FleetEntry[]): Promise<Selection>;
/**
 * Confirm the user wants to proceed with syncing the selected repos.
 */
export declare function confirmSync(selection: Selection): Promise<boolean>;
//# sourceMappingURL=picker.d.ts.map