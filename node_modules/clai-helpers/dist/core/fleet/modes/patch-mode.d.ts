/**
 * Patch mode: Ephemeral clone → sync → diff → write .patch file.
 * Never mutates GitHub.
 */
import type { FleetEntry, SyncResult } from "../types.js";
type FetchLike = (input: string | URL, init?: RequestInit) => Promise<Response>;
/**
 * Sync a repo by generating a .patch file.
 *
 * Flow:
 * 1. Ephemeral clone
 * 2. Run existing sync pipeline against the clone
 * 3. If no changes → no-op
 * 4. Generate diff
 * 5. Write to <patchOutputDir>/<owner>__<repo>.patch
 */
export declare function syncPatch(entry: FleetEntry, _auth: string, latestRef: string, patchOutputDir: string, _fetchFn?: FetchLike): Promise<SyncResult>;
export {};
//# sourceMappingURL=patch-mode.d.ts.map