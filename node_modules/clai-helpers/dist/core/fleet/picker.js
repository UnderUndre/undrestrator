/**
 * Interactive picker for fleet sync.
 * Wraps @inquirer/prompts checkbox + confirm for repo selection.
 */
import { checkbox, confirm } from "@inquirer/prompts";
/**
 * Present an interactive multi-select checkbox list of fleet entries.
 * Pre-checks entries that have drift (pinned !== latest).
 */
export async function pickEntries(entries) {
    if (entries.length === 0) {
        return { entries: [], source: { kind: "interactive" } };
    }
    const choices = entries.map((e) => {
        const version = e.latestRef && e.latestRef !== "unknown"
            ? `${e.pinnedRef} → ${e.latestRef}`
            : e.pinnedRef;
        return {
            name: `${e.fullName} (${version}${e.hasDrift ? " ⚠" : ""})`,
            value: e,
            checked: false,
        };
    });
    const selected = await checkbox({
        message: "Select repos to sync:",
        choices,
    });
    return { entries: selected, source: { kind: "interactive" } };
}
/**
 * Confirm the user wants to proceed with syncing the selected repos.
 */
export async function confirmSync(selection) {
    if (selection.entries.length === 0)
        return false;
    return confirm({
        message: `Sync ${selection.entries.length} repo(s)?`,
        default: true,
    });
}
//# sourceMappingURL=picker.js.map